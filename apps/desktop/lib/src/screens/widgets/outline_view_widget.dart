import 'package:flutter/material.dart';

import '../../widgets/outline_view.dart';

/// Outline view widget
class OutlineViewWidget extends StatelessWidget {
  const OutlineViewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const OutlineView();
  }
}
